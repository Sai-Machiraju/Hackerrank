 Write a function that takes in an integer  𝑛  and computes  𝑛! . Do this without recursion.



python code:
def factorial_iter(n):
    """Takes in an integer n>0 and returns the product of all integers from 1 to n."""
    # YOUR CODE HERE
    result=1
    while(n!=0):
        result=result*(n)
        n=n-1
    return result    
    raise NotImplementedError()
